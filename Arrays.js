//PUSH METHOD

//adds another entry to the end of array (opposite of the "unshift method")

/*let shoppingOrder = [
   "biscuits",
   "teabags",
   "bread"
];
shoppingOrder.push("pasta")
console.log(shoppingOrder);*/

//POP METHOD

//Removes last entry in the array.

/*let shoppingOrder = [
   "biscuits",
   "teabags",
   "bread",
   "pasta"]

   shoppingOrder.pop()
   console.log(shoppingOrder);*/

//SHIFT METHOD

// removes first entry in the array 

/*let shoppingOrder = [
   "biscuits",
   "teabags",
   "bread",
   "pasta"]

   shoppingOrder.shift()
   console.log(shoppingOrder);*/

//UNSHIFT METHOD

//Used to add a new entry to the beginning of the list. (opposite of push method)

/*let shoppingOrder = [
   "biscuits",
   "teabags",
   "bread",
   "pasta"]

   shoppingOrder.unshift("eggs")
   console.log(shoppingOrder);*/   

//SLICE METHOD

//removes strings in the list from the top down. ADD number at the end.

/*let shoppingOrder = [
   "biscuits",
   "teabags",
   "bread",
   "pasta"]

   console.log(shoppingOrder.slice(2))*/

//SPLICE METHOD

//removes and adds everything after 

/*let shoppingOrder = [
   "biscuits",
   "teabags",
   "bread",
   "pasta",
   "pizza",
   "cheese",
   "butter",
]

shoppingOrder.splice(0,4,"milk")
console.log(shoppingOrder);*/
   
//To understand the uses of arrays
//To understand the syntax of creating an array
//To use a variety of methods to work with arrays


//ACTIVITY ONE 

/*Create a list of your favourite website (3 of them), and
then add another two once you’ve created the list.
Then remove the last website.*/

/*let favouriteWebsites = [
   "wwww.google.com",
   "www.facebook.com",
   "www.youtube.com",
]
favouriteWebsites.push("www.apple.com","www.twittier.com")
favouriteWebsites.pop()
console.log(favouriteWebsites*/

//Used to add a new entry to the beginning of the list. (opposite of push method)

/*let shoppingOrder = [
   "biscuits",
   "teabags",
   "bread",
   "pasta",]

   shoppingOrder.unshift("eggs")
   console.log(shoppingOrder);

let orderCount = 0;*/

/*for (i = 0; i < 6; i--){
   console.log(Math.random()* 49 + 1)*/

